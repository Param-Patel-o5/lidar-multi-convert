# Runtime dependencies (mirrors pyproject.toml [project.dependencies]).
# Install: pip install -r requirements.txt
# Editable dev install (recommended): pip install -e ".[dev]"

ouster-sdk>=0.15.1
laspy>=2.6.0
numpy>=1.19.0
scipy>=1.7.0
tqdm>=4.60.0
rich>=12.0.0
pandas>=1.3.0
scikit-learn>=1.0.0
dpkt>=1.9.7
velodyne-decoder>=3.1.0
